# ALGORITHM GUIDE — LOTTO ULTIMATE 알고리즘 해설서

> 이 문서는 시스템에 구현된 모든 알고리즘의 **원리 · 수식 · 한계 · 코드 위치**를 한 곳에 정리합니다.
> 새 알고리즘을 추가할 때 기존 것과 역할이 겹치는지 여기서 확인하세요.
>
> Creative Director: K.G.B. · Oseong Vibe Coding EduHub

---

## 수학적 전제

- 로또 6/45 1등 확률: **1/8,145,060** — 어떤 알고리즘으로도 변경 불가
- 각 추첨은 **완전 독립 사건** — 과거 데이터의 예측력은 이론적으로 0
- 유일한 수학적 레버: **인기 조합 회피** → 당첨 시 공동당첨자 수를 줄여 기대 배당금을 높임

---

## A. 통계 스코어링 (11종) — `lotto_ultimate.py` / `lotto_engine.js`

모든 점수는 0~1로 정규화(min-max)되어 가중 합산됩니다.

### 1. 빈도 점수 (Frequency) — 가중 1.0
- **원리**: 전체 기간 출현 횟수. 많이 나온 번호가 높은 점수.
- **수식**: `score[b] = norm(Σ A[t,b])`
- **한계**: 전체 기간을 균일하게 보므로 최근 변화에 둔감 → recency가 보완.
- **코드**: `score_frequency(A)` / `scoreFrequency(A)`

### 2. 최근 빈도 (Recency) — 가중 1.2
- **원리**: 최근 50회차의 출현 빈도에 지수 가중(최근일수록 가중치 ↑).
- **수식**: `w[i] = exp(linspace(-2, 0, 50))`, `score[b] = norm(Σ w[i]·A[t,b])`
- **왜 가중 1.2**: 가장 최근 정보를 약간 더 반영하겠다는 설계 결정. 과적합 위험이 있으므로 다중 윈도우와 균형.
- **코드**: `score_recency(A, n=50)` / `scoreRecency(A, 50)`

### 3. 갭 점수 (Gap) — 가중 1.0
- **원리**: 마지막 출현 이후 경과 회차. 오래 안 나올수록 점수 ↑ (평균 회귀 가정).
- **수식**: `gap[b] = len(A) - 1 - last_appearance[b]`, `score = norm(gap)`
- **한계**: 선형이라 극단적 미출현을 과대평가 → 시그모이드 콜드가 보완.
- **코드**: `score_gap(A)` / `scoreGap(A)`

### 4. Z-Score — 가중 0.8
- **원리**: 기대 출현 횟수 대비 표준화 편차. 콜드번호 우선(부호 반전).
- **수식**: `z[b] = -(f[b] - μ) / σ`, μ = 전체 평균, σ = 모집단 표준편차
- **왜 콜드 우선**: 인기 번호(많이 나온 번호)를 사람들이 더 선호하므로, 콜드를 가산하면 인기도 회피와 방향이 일치.
- **코드**: `score_zscore(A)` / `scoreZscore(A)`

### 5. 시그모이드 콜드번호 재평가 — 가중 0.7
- **원리**: 갭 점수의 비선형 버전. 로지스틱 함수로 상한을 둬 극단 미출현 과대평가 방지.
- **수식**: `score[b] = norm(1 / (1 + exp(-k·(gap[b] - c))))`, k=0.3, c=45/6≈7.5
- **c의 의미**: 기대 재출현 간격. 갭이 c를 넘으면 점수가 0.5 이상, 그 아래면 이하.
- **코드**: `score_sigmoid_cold(A)` / `scoreSigmoidCold(A)`

### 6. 모멘텀 — 가중 1.0
- **원리**: 단기(20회) 출현율 / 장기(100회) 출현율. 비율 > 1이면 상승 트렌드.
- **수식**: `mom[b] = mean(A[-20:, b]) / (mean(A[-100:, b]) + ε)`
- **한계**: 독립 추첨이므로 "트렌드"는 이론적으로 무의미. 그러나 인기도 역산에서 "최근 나온 번호를 사람들이 피한다"는 행동 패턴이 있어 간접적 역할.
- **코드**: `score_momentum(A)` / `scoreMomentum(A)`

### 7. 쌍빈도 (Pair Frequency) — 가중 0.8
- **원리**: 다른 번호와 동시에 나온 총 횟수. 동반출현이 많은 번호가 높은 점수.
- **수식**: `P = A^T·A` (대각 0), `score[b] = norm(Σ_j P[b,j])`
- **코드**: `score_pair(A)` / `scorePair(P)`

### 8. 마르코프 전이확률 — 가중 1.0
- **원리**: 직전 회차의 번호에서 다음 회차 번호로의 조건부 전이확률. 라플라스 평활(α=1).
- **수식**: `T[a,b] = (count(a→b) + 1) / (Σ_j count(a→j) + 45)`, 마지막 회차 조건부 합.
- **한계**: 독립 추첨이므로 진짜 전이 패턴은 없음. 그러나 "직전에 나온 번호를 사람들이 다시 고르는" 행동을 잡아 인기도 회피에 기여.
- **코드**: `score_markov(hist)` / `scoreMarkov(hist)`

### 9. Lift 연관규칙 — 가중 0.8
- **원리**: 번호 쌍의 동시출현이 독립 기대보다 높은 정도. Lift = P(A∩B) / (P(A)·P(B)).
- **수식**: `lift[b] = Σ_{j≠b} (P[b,j]/n) / (p[b]·p[j] + ε)`, score = norm(lift)
- **쌍빈도와의 차이**: 쌍빈도는 절대 횟수, Lift는 기대치 대비 초과분. 자주 나오는 번호끼리는 쌍빈도가 높지만 Lift는 낮을 수 있음.
- **코드**: `score_lift(A)` / `scoreLift(A, P)`

### 10. 다중 윈도우 민감도 — 가중 0.9
- **원리**: 30/50/100/전체 기간의 출현율을 각각 정규화 → 평균. 모든 윈도우에서 높은 번호 = 안정 신호.
- **왜 필요**: 30회에서만 핫한 번호는 일시적 변동일 수 있음. 모든 기간에서 꾸준한 번호를 우선하면 과적합 위험 감소.
- **코드**: `score_multi_window(hist)` / `scoreMultiWindow(hist)`

### 11. Dirichlet 사후확률 — 가중 0.6
- **원리**: 균등 사전(α₀=1)에 관측 빈도를 더해 베이지안 사후 기대값 계산.
- **수식**: `post[b] = (f[b] + α₀) / (Σf + 45·α₀)`
- **빈도 점수와의 차이**: 빈도는 순수 데이터, Dirichlet은 "45개 번호는 원래 균등하다"는 사전 믿음이 포함. 데이터가 적을 때 극단 추정을 억제.
- **왜 가중 0.6**: 1241회차 데이터에서는 사전의 영향이 미미하므로 보조 역할.
- **코드**: `score_dirichlet(hist)` / `scoreDirichlet(hist)`

---

## B. 머신러닝 (4종) — `lotto_ultimate_ml.py`

### 12. Random Forest + Gradient Boosting
- **원리**: (회차, 번호) 쌍을 표본으로, 7개 피처(최근10/30/100출현율, 갭, 모멘텀, 직전출현, 번호위치)로 "다음 회차에 이 번호가 나오는가" 이진 분류.
- **두 모델 평균**: RF는 배깅(분산 감소), GB는 부스팅(편향 감소). 평균하면 안정.
- **검증**: 출현확률 평균이 이론값(6/45=0.133)에 수렴하면 과적합 없음.
- **가중**: ML 점수 = 1.5 (다른 점수의 정보를 종합하므로 높게)
- **코드**: `train_ml(A)`, `score_ml(A, models)`

### 13. 유전 알고리즘 (적응형 돌연변이)
- **원리**: 조합 단위 적합도(볼점수 − λ·인기도 + 엔트로피 + 커뮤니티 다양성) 진화 최적화.
- **핵심**: 돌연변이율이 개체군 다양성에 따라 자동 조절(0.03~0.40). 다양성 < 0.6이면 ↑, 높으면 ↓.
- **코드**: `genetic_optimize()` (Python) / `LUX.geneticOptimize()` (JS)

### 14. KMeans 클러스터링 + Silhouette
- **원리**: 후보 조합을 45차원 이진 벡터로 표현 → KMeans(k=세트 수)로 군집화 → 각 군집에서 최고점 1개 선택.
- **목적**: 추천 세트 간 다양성 보장. 10세트가 모두 비슷한 번호면 리스크 집중.
- **코드**: `generate_sets()` 내 KMeans / JS `kmeans()`

### 15. VotingClassifier (설계서 포함, 현재 RF+GB 앙상블로 대체)
- **상태**: RF+GB 확률 평균이 동일한 역할을 수행. 별도 VotingClassifier 래퍼는 미구현.

---

## C. 네트워크 분석 — `lotto_ultimate_ml.py`

### 16. 동반출현 네트워크 + 스펙트럴 커뮤니티 (8개)
- **원리**: 동반출현 가중 그래프(45×45) → 스펙트럴 클러스터링 → 8개 커뮤니티.
- **허브 점수**: 가중 차수 / 기대 차수. 허브 번호는 다른 번호들과 골고루 나온 번호.
- **조합 생성 활용**: 조합이 여러 커뮤니티에 걸치는지를 적합도에 반영(+0.02/커뮤니티).
- **코드**: `network_analysis(A, k=8)`

---

## D. 인기도 역산 — `lotto_ultimate.py` / `lotto_engine.js`

### 17. quasi-Poisson 인기도 모델
- **원리**: Phase 3에서 발견된 4개 유의 피처로 조합의 "대중 인기도"를 추정.
- **피처와 계수**:
  - `sum` (합계): −4.9%/SD — 합이 높을수록 덜 인기
  - `same_row_max` (같은 OMR 행 최대): −7.4%/SD — 한 행에 몰리면 덜 인기
  - `tail_digit_repeat` (끝수 반복): +5.9%/SD — 끝수가 반복되면 더 인기
  - `prev_overlap` (직전 회차 겹침): −1.4%/SD — 직전 번호와 겹치면 덜 인기
- **추가 규칙**: 생일 범위(1~31) 집중 가산, 행운수(3,7,13,21,27) 가산.
- **코드**: `popularity_index(c, prev)` / `LU.popularityIndex(c, prev)`

---

## E. 군집 지능 (3종) — `lotto_ultimate_swarm.py` / `lotto_engine_opt.js`

### 18. PSO (입자 군집 최적화)
- **원리**: 45차원 연속 위치에서 상위 6개 좌표를 번호로 해석. 관성(0.9→0.4), 개인/전체 최적 추종.
- **연속→이산 해석**: 계단형 지형 → 시작 위치를 볼 점수 근처에 배치해 완화.
- **코드**: `pso_optimize()` / `LUX.psoOptimize()`

### 19. ACO (개미 군집 최적화)
- **원리**: 페로몬(45×45, 초기값=동반출현) 위에서 τ^α·η^β 확률로 6노드 궤적 선택.
- **증발 ρ=0.15**: 오래된 페로몬이 사라져 새 경로 탐색 유지.
- **엘리트 강화**: 세대 최고 개미의 궤적에 3배 페로몬 → 수렴 가속.
- **코드**: `aco_optimize()` (Python만, JS 미이식)

### 20. 점균류 관 네트워크 (Physarum/Tero 모델)
- **원리**: 45개 노드의 관 굵기가 유량에 비례해 성장, 미사용 관은 쇠퇴. 무작위 출발·도착을 반복하면 자주 쓰이는 간선만 생존.
- **수학**: 라플라시안 선형계로 압력 풀어 유량 계산, `D = 0.9·D + 0.4·|Q|/max(|Q|)`.
- **코드**: `slime_optimize()` (Python만)

---

## F. 조합 필터 (8종) — `lotto_ultimate.py` / `lotto_engine.js`

| # | 필터 | 기준 | 실제 당첨 커버율 |
|---|---|---|---|
| 21 | 합계 | 100~175 | ~85% |
| 22 | AC값 | ≥ 7 | ~80% |
| 23 | 홀짝 비율 | 2:4, 3:3, 4:2 | ~85% |
| 24 | 저고 비율 | 저(1~22):고(23~45) = 2:4~4:2 | ~85% |
| 25 | 연속번호 | 3연속 이상 금지 | ~95% |
| 26 | 끝수 분산 | 동일 끝수 3개 이상 금지 | ~90% |
| 27 | 번호대 분산 | 10단위 구간 3개 이상 커버 | ~90% |
| 28 | 인기 패턴 하드 제외 | 등차수열, 단일행 4+, 모두 ≤31 | 거의 100% |

- **코드**: `passes_filters(c)` / `LU.passesFilters(c)`

---

## G. 진정 난수 시드 (4소스) — `lotto_ultimate_swarm.py`

| # | 소스 | 원리 | 폴백 순서 |
|---|---|---|---|
| 29 | ANU 양자난수 (QRNG) | 광자 빔 분할의 양자 요동 | 1순위 |
| 30 | random.org 대기 잡음 | 라디오 수신기의 대기 잡음 | 2순위 |
| 31 | USGS 지진파 | 최근 1시간 지진 규모·위치·시각의 SHA-256 | 3순위 |
| 32 | OS CSPRNG | `/dev/urandom` (Linux) / `CryptGenRandom` (Windows) | 최종 폴백 |

- **목적**: "예측"이 아니라 "소프트웨어 유사 난수의 패턴성 제거".
- **화이트닝**: 어떤 소스든 SHA-256으로 한 번 해시 → 편향 제거.

---

## H. VRF (검증가능 난수) — `lotto_ultimate_swarm.py` / `lotto_engine_opt.js`

### 33. SHA-256 해시체인 + 거부 샘플링
- **커밋**: `SHA256(seed ‖ round ‖ "COMMIT")[:16]` → 추첨 전 공개
- **번호 추출**: `SHA256(seed ‖ round ‖ set_id ‖ counter)` → 바이트 < 225만 채택 → `mod 45 + 1`
- **균등성**: 225 = 45×5 → 모듈로 바이어스 0. 20만 표본 검증 max/min = 1.05.
- **재현성**: 같은 시드 + 회차 → 동일 번호. Python↔JS 바이트 단위 일치 검증 완료.

---

## I. 검증 체계 (7종) — `lotto_ultimate.py` / `lotto_ultimate_ml.py`

### 34. 15종 무작위성 검정 배터리 (Holm-Bonferroni)
- χ² 균등성, 런 검정, 자기상관, 엔트로피, 갭 분포, 쌍빈도 χ², 행렬 순위, LZ 복잡도, 직렬 검정, 합계 KS, 끝수 균등, 구간 균등, 연속쌍 빈도, 홀짝 분포, 정상성
- **다중비교 보정**: Holm-Bonferroni. 15개 중 하나가 p<0.05인 확률은 ~54%이므로 보정 필수.

### 35. Walk-forward 백테스트
- 시점 t 이전 데이터만으로 예측 → t의 실제 번호와 비교. 데이터 누수 없음.

### 36. 랜덤 대조군 100세트
- 매 라운드 무작위 100세트의 평균 일치를 대조군으로 사용.

### 37. 부트스트랩 95% CI
- 2000회 리샘플링으로 신뢰구간 산출.

### 38. 필요 표본 크기 계산
- **수식**: `n = 2·(z_α + z_β)² / d²`, d = Cohen's d (관측 효과 크기)
- **해석**: d=0.316 → 약 158회차 필요 (검정력 80%). "아직 결론 내리기 이르다"의 정량적 근거.

### 39. PredictionTracker
- 사전등록(추첨 전) → 사후채점(CSV 업로드 시 자동). 사후 합리화 차단.
- **코드**: `register_predictions()`, `score_predictions()`

### 40. 볼당 최대 등장 제약
- `max_per_ball = ceil(세트수 × 0.4)`. 고득점 번호 1개에 과도하게 베팅하는 것을 방지.
- **코드**: `_greedy_select()` / JS `generateSets()`

---

## J. 앙상블 합의 — `lotto_ultimate_swarm.py` / `lotto_engine_opt.js`

### 41. 탐색기 투표 + 동시 수렴 검출
- GA·PSO·ACO·SLIME 후보의 번호별 출현 빈도를 투표 → 상위 10볼 식별.
- ≥2개 탐색기가 동일 조합에 수렴하면 "적합도 지형의 진짜 봉우리" 후보로 우선.

---

*이 문서는 코드가 변경될 때마다 갱신되어야 합니다. 마지막 갱신: 2026-09-22*
